import React, { Component } from 'react'
import { connect } from 'react-redux'

class ListPolls extends Component {
  render() {
    return (
      <div>
        ListPolls
      </div>
    )
  }
}

export default connect()(ListPolls)
